services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_container
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  mongo-express:
    image: mongo-express
    container_name: mongo_express_container
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: password
      ME_CONFIG_MONGODB_URL: mongodb://admin:password@mongodb:27017/?authSource=admin
    depends_on:
      - mongodb

  api:
    image: node:20-slim
    container_name: fastify_api
    working_dir: /bnj-text-analyzer
    volumes:
      - .:/bnj-text-analyzer
    env_file:
      - ./apps/api/.env
    ports:
      - "3001:3001"
    depends_on:
      - mongodb
    command: >
      sh -c "npm install -g pnpm && pnpm install --force && pnpm --filter @workspace/api dev"

volumes:
  mongodb_data: